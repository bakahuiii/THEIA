# THEIA 产品方向

> 产品规划评估，基于 2026-08 的现有代码库。本文的优先方向不是当前功能清单；当前实现以 [文档中心](docs/README.md)、代码和测试为准。

---

## 现状判断

数据管道是真实优势：教务与课程平台同步、手动触发的 AI 作业工作流、选课目标与执行队列均已有基础实现。
问题在于所有数据目前流向被动展示——本质上还是"更好看的教务系统"。

产品目标是让现有数据管道从“展示数据”延伸到“推断与决策”，同时保持用户确认与数据来源可追溯。

---

## 三个优先方向

### 1. 风险引擎

把已有的 `AcademicProgress` + `Grade` 数据拿来算推断，而不只是展示：

- 距大一→大二升级线还差几必修学分（≥2/3 门槛）
- 距大二→大三还差多少（≥3/4 门槛）
- 按培养方案和明确规则计算的学分缺口与情景结果
- 最高 GPA 是否有跌破 2.00 的风险（学位预警）
- 哪门必修如果挂了会触发退学预警

首批确定性信号不需要新数据源，可从现有 state 推导。规则来源、数据新鲜度和未知项必须随结果展示，不能把缺失数据包装成概率或确定结论。

### 2. "今天该干什么"合成面

把以下信号 merge 成一个有优先级的行动列表，每条说明为什么它排在这里：

- 下场考试倒计时
- 作业截止紧迫度
- 体测窗口剩余天数（第 5–12 周）
- 升级线学分缺口
- 第二课堂认定窗口（秋季前 3 周）

不需要 AI，需要一个优先级公式。目标是打开 THEIA 能直接看到"现在最重要的一件事是什么"。

### 3. 选课沙盘变成决策工具

现在的 `CourseSelection` 是抢座机器，不是决策工具。缺的逻辑：

- 候选课 × 培养方案缺口匹配（这门课能填哪个学分坑）
- 候选课 × 当前课表冲突检测
- 候选课 × 历史成绩分布推断难度
- 输出：推荐排序 + 理由

数据都在（成绩历史、培养方案进度、当前课表），只差把它们组合起来。

---

## 地图

搁置，不砍。PC 端移到导航靠后位置。手机版发布后带导航是真实需求。

---

## 学习工具的数据接入

`FitnessCalc` 已接入健康云归档，可离线切换已缓存年度。`SecondClassCalc`、`WarningCalc` 和 `InnovationCalc` 仍以手工输入为主；后续只有在来源、规则版本和失败语义明确时再接自动填充，不能用推断值冒充学校认定。

---

## AI 功能定位

两条线，互补不竞争。

**作业线（规划目标）**
当前已能手动准备工作包并触发作业处理、笔记和论文草稿生成。规划中的后台处理包含可关闭的队列、去重、失败状态和完成通知；处理结果仍由用户审核并在学校平台手动提交。

**顾问线（规划目标）**
P0 已实现无模型的逐领域质量判断、证据/typed claim、数据质量风险、作业与考试时间记录风险以及确定性 Agenda。完整“今天该干什么”界面、培养方案/GPA/选课复杂规则和模型解释层尚未实现。后续优先级计算仍必须是可测试的确定性逻辑；AI 只负责解释已计算结果，不编造校规、未来成绩或毕业概率。

两条线的数据流、上下文构建、模型分配见 [AI_DIRECTION.md](./AI_DIRECTION.md)。
