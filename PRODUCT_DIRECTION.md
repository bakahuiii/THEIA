# THEIA 产品方向

> 产品规划评估，基于 2026-08 的现有代码库。本文的优先方向不是当前功能清单；当前实现以 [文档中心](docs/README.md)、代码和测试为准。

---

## 现状判断

数据管道仍是真实优势：教务与课程平台同步、手动触发的 AI 作业工作流、选课目标与执行队列均已有基础实现。P0-P3 已在其上落地无模型的本地确定性决策层，包括今日行动、学业分析和只读选课排序；THEIA 已不再只有被动展示。

P4 模型顾问首发与 P5 通知/邮件按需上下文已经接入：模型请求经过最小授权披露、冻结 catalog 和严格叙述校验，顾问线程只保存在内存中。当前主要缺口已经转为持久会话、多轮摘要、完整评测与发布验证；有界工具 Agent 仍是独立后续阶段。后续仍要保持用户确认、数据来源可追溯，并让模型只能解释已验证的本地事实。

---

## 三个优先方向

### 1. 风险引擎：已落地，继续补可信规则源

把已有的 `AcademicProgress` + `Grade` 数据拿来算推断，而不只是展示：

- 基于培养方案 AND/OR 树计算可确认的学分缺口
- 对比学校 GPA、档案 GPA 与本地核对值，并显示来源差异
- 按培养方案和明确规则计算的学分缺口与情景结果
- 只在课程号或官方节点关系明确时关联失败课程影响
- 注入带来源和版本的规则后，计算升级线的纯算术距离

上述本地分析已经实现，并显示规则来源、数据新鲜度和未知项。production `upgradeRule` 当前默认是 `null`，UI 会显示“尚未配置”；在没有可信规则来源与版本前，不得把缺失数据包装成概率，也不得宣称升级、毕业、退学或学籍结论。

### 2. "今天该干什么"合成面

P1 已在概览页显示 Top 1、顾问页显示 Top 7，并为每项展示可复算的优先级分量、数据质量和证据。当前接入的信号包括：

- 数据读取失败、需登录、过期、部分或完整性未知；
- 下场考试倒计时与无法确认的考试时间；
- 作业截止紧迫度与无法解析的截止时间；
- 官方校历中可确认的选课窗口；
- 满足严格数据门槛的学业缺口。

体测周和第二课堂认定窗口尚未接入 Agenda，不能写成已实现信号。优先级继续由可测试的确定性公式计算，不交给模型自由排序。

### 3. 选课沙盘变成决策工具

P3 已将 `CourseSelection` 扩展为与抢课执行隔离的只读决策工具：

- 候选课与培养方案缺口匹配；
- 当前课表冲突及数据不足时的 unknown 降级；
- 已完成、当前已选和历史修读识别；
- 稳定推荐排序、理由与证据。

历史信息只统计匹配记录和其中可用的数值绩点，不推断课程难度或未来成绩。`save-target`、`view-details`、`open-confirmation` 仍只是非执行 proposal 数据，真正保存目标、确认或抢课继续走原有用户操作和业务服务。

---

## 地图

搁置，不砍。PC 端移到导航靠后位置。手机版发布后带导航是真实需求。

---

## 学习工具的数据接入

`FitnessCalc` 已接入健康云归档，可离线切换已缓存年度。`SecondClassCalc`、`WarningCalc` 和 `InnovationCalc` 仍以手工输入为主；后续只有在来源、规则版本和失败语义明确时再接自动填充，不能用推断值冒充学校认定。

---

## AI 功能定位

两条线，互补不竞争。

**作业线（规划目标）**
当前已能手动准备工作包并触发作业处理、笔记和论文草稿生成。规划中的后台处理包含可关闭的队列、去重、失败状态和完成通知；处理结果仍由用户审核并在学校平台手动提交。

**顾问线（当前首发）**
P0-P3 已实现无模型的逐领域质量判断、证据/typed claim、确定性 Agenda、完整“今天该干什么”界面、培养方案/GPA 分析、纯算术 What-if 和只读选课决策。P4 已实现 `ContextBuilder`、授权披露、`AdvisorRuntime`、OpenAI-compatible Provider、严格模型叙述校验、内存线程、取消和模型路由；P5 已实现用户点选的通知/邮件安全投影、逐封正文授权和纯内存词法索引。优先级计算仍必须是可测试的确定性逻辑；AI 只负责解释已计算结果，不编造校规、未来成绩或毕业概率，也不执行学校操作。准确边界见 [P1-P3 本地工作台说明](docs/ai/17-advisor-p1-p3-local-workbench.md)和 [P4-P5 模型运行时说明](docs/ai/18-advisor-p4-p5-model-runtime.md)。

两条线的数据流、上下文构建、模型分配见 [AI_DIRECTION.md](./AI_DIRECTION.md)。
