# THEIA 产品待办

本文件只列尚未形成完整用户闭环的产品方向，不重复已经实现的体测归档、分片存储、本机 API 或导出能力。实现细节和优先级见 [产品方向](PRODUCT_DIRECTION.md) 与 [AI 方向](AI_DIRECTION.md)。

## P4-P5 顾问工程后续

- 为当前内存线程设计独立于 `CampusState` 的加密 `AdvisorStore`，补齐 nonce/AAD、损坏隔离、删除、恢复、密钥轮换和旧 evidence 生命周期；完成前不持久化顾问线程。
- 实现受 revision/domain digest 和输入预算约束的可丢弃多轮摘要；当前旧轮次原文不重新发送给模型。
- 在真实产品需要明确后，再把纯内存词法索引接入点选之外的本地检索；不得后台扫描整个邮箱，不得索引未授权正文或附件二进制。
- 将有界只读工具 Agent 作为独立权限阶段设计和验收，不把 `suggestedActionIds` 或现有固定动作误当成模型工具执行权。
- 扩充版本化模型评测 corpus、Provider 兼容性矩阵和桌面/窄屏视觉验证；没有全量门禁、隔离 packaged smoke 和真实 Provider 验证前不宣称发布完成。
- 为 production `upgradeRule` 建立可信来源、版本与配置流程；未配置时继续显示“尚未配置”。
- 建立版本化评测 corpus 和可复现实验工具，实测 overview 冷/热 p50、p95 与额外 RSS；规划文档中的门槛在工具验收前不算已达成。

## 作业自动编排

- 为现有的工作包准备、模型处理、答案写入和 PDF 渲染能力增加可关闭的后台队列、去重、状态恢复和用户通知。
- 自动编排只准备本地结果或填写待审核页面；学校平台的最终提交继续由用户确认。
- 在宣称可无人值守前，补齐失败重试、模型配置缺失、附件解析失败、输出校验和桌面通知的端到端验证。

## 发布与兼容

- 每次版本发布从 `package.json` 读取版本，不在待办中固定当前版本号或测试数量。
- 继续保留旧数据迁移与恢复入口；只有在明确迁移策略、备份和验证后才能移除兼容路径。
- 安装包必须经过实际启动、关键 IPC、数据迁移和产物完整性检查后，才能标记发布完成。
