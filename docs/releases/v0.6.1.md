# THEIA v0.6.1

## 本次收口

- 补齐 GitHub 自动更新运行时、IPC 入口和 About 页更新按钮，正式 Windows 安装包现在可以检查、下载并安装更新。
- 更新包装配置与回归测试，明确了支持环境、未打包环境回退和发布检测路径。
- 把功能文档拆成 docs/features/，并把每个页面的说明补到按钮、状态、空态、异常和代码入口的粒度。
- 保持现有校园功能边界不变：只读读取、本机保存、最终提交仍由用户确认。

## 验收结果

- node --test tests/github-update-runtime.test.mjs tests/packaging-config.test.mjs：9/9 通过。
- npm run lint：通过。

## 范围说明

- 地图功能仍按现状保留，不作为本次发布阻断项。
- 移动端工作仍在其它工作区推进，不纳入本版本验收。
- 当前版本说明以仓库代码与文档为准；正式发布产物和安装包校验值由发布流程生成。
