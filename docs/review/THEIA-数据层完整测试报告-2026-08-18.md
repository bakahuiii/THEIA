# THEIA 数据层全量抓取与本地存储报告

**报告日期：** 2026-08-18  
**唯一抓取证据：** `H:\work\THEIA\.crawl-rendered-20260818-full-clean`  
**本地数据根目录：** `C:\Users\Administrator\AppData\Roaming\THEIA`  
**范围：** API 与认证浏览器的完整只读抓取、归一化结果、培养计划 PDF/JSON、当前分片存储。  
**未执行：** `npm`、`node --test`、构建、lint、学校端写操作、桌面或浏览器控制。

## 结论

本次真实抓取已经完整跑过 API 和认证浏览器两条通道，不是单元测试替代品。两条通道均完成了培养计划读取：`academic-plan` 均为 `0 records + 1 PDF + complete`。因此，**当前 API 和浏览器都能获取培养计划**。

当前本地数据存储结构完整：主、备 `theia-sharded-store/v1` manifest 都引用 19 个分片；每个引用的对象文件存在，fragment 的 `schema`、`kind` 和声明 digest 与各自 manifest 一致。主 manifest 是当前提交，备 manifest 是上一版健康提交，二者的记录数量一致但部分 path/digest 随最近提交更新；这属于备份轮换状态，不是损坏。培养计划只有当前专业的一份本地 PDF，并已转换为 14 页的 JSON 分片。

本次同时发现一个必须保留的事实：同一培养计划附件 ID 在 API、浏览器、当前本地资产中分别出现了三个不同的二进制 SHA-256，三者字节数均为 26,721。这轮抓取没有把任意一个结果写回本地，因此没有用不一致的数据覆盖已验证的本地 PDF/JSON；但这也意味着不能把三份二进制写成“完全相同”。

## 抓取执行

抓取产物的 UTC 时间为 `2026-08-17T19:16:04Z` 至 `2026-08-17T19:17:46Z`，即北京时间 2026-08-18 03:16 至 03:17。

| 通道 | 结果 | 耗时 | 说明 |
| --- | --- | ---: | --- |
| 直接 API | 完成 | 12,290 ms | 读取当前认证会话下的教务数据。 |
| 认证浏览器渲染 | 完成 | 87,164 ms | 已连接、未要求重新认证、无顶层错误。 |
| 合计 | 完成 | 99,454 ms | 浏览器请求日志共 118 条；只读取学校端数据。 |

两条通道的归一化计数如下。`partial` 表示该域的源响应不完整或不符合预期，不会使其他成功域失效。

| 数据域 | API | 浏览器 |
| --- | ---: | ---: |
| profile fields | 3 | 3 |
| terms | 9 | 9 |
| courses | 35，partial | 35，partial |
| schedule | 62 | 62 |
| grades | 40 | 40 |
| exams | 20 | 20 |
| selected courses | 38 | 38 |
| notices | 35 | 35 |
| academic progress | 58 categories / 1 root | 57 categories / 7 roots |
| 培养计划 | 0 records / 1 PDF / complete | 0 records / 1 PDF / complete |
| 成绩明细 | 141 / complete | 141 / complete |
| 空闲教室 | 181 / complete | 181 / complete |

`graduation-audit` 在两条通道都返回 `partial`，原因是 `graduation-audit_unexpected_payload`。浏览器的 `exam-extra` 也为 `partial`，原因是 `exam-extra_unexpected_payload`。这些失败没有覆盖课程、课表、成绩、培养计划等成功结果。

学业预警和毕设不在当前 active state，也不作为本报告的抓取、归一化或持久化域。

## 培养计划

当前本地只保留本专业的一份培养计划 PDF，且它和教学进程表、周历放在同一目录：

`C:\Users\Administrator\AppData\Roaming\THEIA\academic-calendar\assets`

该目录中与 PDF 相关的文件为：当前培养计划 PDF、`teaching_schedule_current.pdf`、`weekly_calendar_current.pdf`。没有第二份培养计划 PDF。

| 项目 | 当前本地资产 | 本轮 API 抓取 | 本轮浏览器抓取 |
| --- | --- | --- | --- |
| 附件 ID | `b2e9b6797873b86f885acb52` | `b2e9b6797873b86f885acb52` | `b2e9b6797873b86f885acb52` |
| HTTP / 域结果 | 已缓存资产 | 培养计划 complete | 培养计划 complete，PDF HTTP 200 |
| 字节数 | 26,721 | 26,721 | 26,721 |
| SHA-256 | `386d5f7656ae57507790727235fb9eaf9bf39ec8b1b5fb40cb94a851bbd8bbf5` | `f5fd6e41266d90e7b56426223afc26305b099b9ae11a68f2fbe0e37887b18822` | `0ded23062559e0ba52643425bbe75b3ab3fe87276f82162a5ea4f412725d2b12` |

本地资产的文件头为 `%PDF-`。对应 `academic/plan-document` JSON 分片记录：

| 项目 | 实测值 |
| --- | --- |
| schema | `theia-academic-plan-document/v1` |
| parser version | `2026-08-18.1` |
| 页数 | 14，14 页均有文字 |
| 提取字符数 | 18,004 |
| 标题 | `2024高分子材料与工程执行计划` |
| 学制 | 4 年 |
| 最低毕业学分 | 171 |
| 文本摘要 | `dfe95a2207511ece1e7510a5e216d3035e4c775e2142482f2935b9438b12b90a` |

PDF 本身有文字层，本次没有使用 OCR fallback。PDF 二进制差异尚未证明是内容差异还是 PDF 内部元数据差异；在做页文本/规范化内容比对前，不应把它们合并为同一份文件，也不应以任意一个覆盖本地已验证资产。

## 本地存储核验

| 检查项 | 结果 |
| --- | --- |
| 主 manifest | `theia-sharded-store/v1`，19 个 fragment 引用 |
| 备 manifest | `theia-sharded-store/v1`，19 个 fragment 引用 |
| 主备引用 | 两边均为 19 个 key；主备 path/digest 不完全相同，备 manifest 为上一版健康提交，revision 不同且记录数量一致 |
| fragment 文件 | 19/19 存在 |
| fragment 元数据 | 19/19 的 wrapper schema、kind、声明 digest 与 manifest 引用匹配 |
| 培养计划元数据 | `records=0`、`attachments=1`、`complete` |
| 培养计划 JSON | 已独立存入 `academic/plan-document` 分片，不混入通用附件元数据 |

这次是只读健康核验，没有修改 manifest、PDF、JSON 或学校端数据。

## 数据边界

培养计划的原始域保持 PDF-only：`records` 始终为空，附件元数据中只保留当前专业的一份 PDF。页面文字只存在独立的 `academic/plan-document` 分片，供本地分片读取；不会进入通用 feed。

抓取通道的 fallback 只有在一个通道对应域失败时才会触发。本次 API 和浏览器都成功取得培养计划，因此没有发生 fallback，也不能把本次结果写成“fallback 已在现场触发”。

## 证据文件

- `H:\work\THEIA\.crawl-rendered-20260818-full-clean\run-meta.json`
- `H:\work\THEIA\.crawl-rendered-20260818-full-clean\api-result.json`
- `H:\work\THEIA\.crawl-rendered-20260818-full-clean\browser-result.json`
- `H:\work\THEIA\.crawl-rendered-20260818-full-clean\comparison.json`
- `C:\Users\Administrator\AppData\Roaming\THEIA\data\manifest.json`
- `C:\Users\Administrator\AppData\Roaming\THEIA\data\manifest.json.bak`

本报告不引用 7 月 9 日或其它历史抓取目录作为本次结论的证据。
